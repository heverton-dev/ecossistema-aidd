ROLE: Builder. Ecosystem AIDD. Ticket 1. Target tool: aidd-melhoria.
DIMENSION: D3
DELIVER: .agents/skills/aidd-melhoria/scripts/isolamento.py

STEPS:
- Write test first: fail with exit 1 when tool writes outside docs/ or outside ephemeral Git Worktree.
- Implement .agents/skills/aidd-melhoria/scripts/isolamento.py. Run analysis inside isolated Git Worktree.
- Block write I/O on repo root. Allow only docs/melhorias/.
- Run tests/test_melhoria_isolamento.py. Assert exit 0.

RULES:
1. Write failing test first. Run. Assert exit 1.
2. Implement. Zero stubs. Real pytest tests. Run. Assert exit 0.
3. Work inside ephemeral Git Worktree only.
4. Capture real exit code: redirect to file, read $? same line. Never trust pipes.
5. NEVER edit docs/auditoria/CONFIG-EXECUCAO-USUARIO.json.
6. Deliver exactly: .agents/skills/aidd-melhoria/scripts/isolamento.py
