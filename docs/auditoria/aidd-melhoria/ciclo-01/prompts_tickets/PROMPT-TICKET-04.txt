ROLE: Builder. Ecosystem AIDD. Ticket 4. Target tool: aidd-melhoria.
DIMENSION: D11
DELIVER: .agents/skills/aidd-melhoria/scripts/fallback.py

STEPS:
- Write test first: simulate manifest read failure or LLM timeout. Current code must crash abruptly.
- Implement .agents/skills/aidd-melhoria/scripts/fallback.py. Add autonomous retry with backoff.
- Add graceful fallback and pipeline stop. Record failure without losing state.
- Run tests/test_melhoria_excecoes_fallback.py. Assert retry and fallback logs appear.

RULES:
1. Write failing test first. Run. Assert exit 1.
2. Implement. Zero stubs. Real pytest tests. Run. Assert exit 0.
3. Work inside ephemeral Git Worktree only.
4. Capture real exit code: redirect to file, read $? same line. Never trust pipes.
5. NEVER edit docs/auditoria/CONFIG-EXECUCAO-USUARIO.json.
6. Deliver exactly: .agents/skills/aidd-melhoria/scripts/fallback.py
