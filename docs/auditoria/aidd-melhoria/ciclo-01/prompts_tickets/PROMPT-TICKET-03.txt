ROLE: Builder. Ecosystem AIDD. Ticket 3. Target tool: aidd-melhoria.
DIMENSION: D8
DELIVER: .agents/skills/aidd-melhoria/scripts/analisador.py

STEPS:
- Write test first: feed malformed JSON or plain text to analytic parser. Assert exit 1.
- Implement .agents/skills/aidd-melhoria/scripts/analisador.py. Replace chat-only processing with deterministic engine.
- Wrap LLM call in strict JSON Schema. Reject unstructured replies. Extract serializable report, e.g. {"relatorio": "..."}.
- Run tests/test_melhoria_motor_deterministico.py. Assert exit 0.

RULES:
1. Write failing test first. Run. Assert exit 1.
2. Implement. Zero stubs. Real pytest tests. Run. Assert exit 0.
3. Work inside ephemeral Git Worktree only.
4. Capture real exit code: redirect to file, read $? same line. Never trust pipes.
5. NEVER edit docs/auditoria/CONFIG-EXECUCAO-USUARIO.json.
6. Deliver exactly: .agents/skills/aidd-melhoria/scripts/analisador.py
