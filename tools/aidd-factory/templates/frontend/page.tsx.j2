export default function Dashboard() {
  return (
    <div>
      <h2 className="text-2xl font-bold mb-4">Dashboard — {{ nicho_nome_exibicao }}</h2>
      <div className="grid grid-cols-1 md:grid-cols-{{ servicos | length }} gap-4">
        {% for servico in servicos %}
        <div className="border rounded-lg p-4">
          <h3 className="font-semibold">{{ servico.nome }}</h3>
          <p className="text-sm text-gray-500">{{ servico.descricao }}</p>
          <a href="/{{ servico.nome_slug }}" className="text-blue-500 text-sm mt-2 inline-block">
            Acessar &rarr;
          </a>
        </div>
        {% endfor %}
      </div>
    </div>
  );
}
