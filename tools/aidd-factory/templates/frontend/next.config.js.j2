/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  async rewrites() {
    return [
      {% for servico in servicos %}
      {
        source: '/api/{{ servico.nome_slug }}/:path*',
        destination: `${process.env.GATEWAY_URL || 'http://localhost:8000'}/{{ servico.nome_slug }}/:path*`,
      },
      {% endfor %}
    ];
  },
};

module.exports = nextConfig;
