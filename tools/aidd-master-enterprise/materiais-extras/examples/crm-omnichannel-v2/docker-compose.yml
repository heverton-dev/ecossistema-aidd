version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=sqlite:///app.db
      - PORT=3000
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  # Descomente para usar PostgreSQL em Produção:
  # postgres:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_DB: app_db
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: secretpassword
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data

# volumes:
#   pgdata:
