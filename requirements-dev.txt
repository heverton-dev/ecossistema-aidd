# Dependências de Desenvolvimento, Qualidade e Testes — Ecossistema AIDD
#
# PLAN-0018 fase 04 (pin-exato-e-hashes): pins exatos (==), sem faixas soltas
# (>=, ~=, <). A instalação real e verificada em CI/pre-commit usa o
# lockfile com hashes criptográficos gerado a partir deste arquivo:
#   uv pip compile requirements-dev.txt --generate-hashes -o requirements-dev.lock
-r requirements.txt

# Testes e Cobertura (pytest/pytest-cov ja vem via -r requirements.txt)
pytest-asyncio==1.4.0
pytest-mock==3.15.1
# Execucao paralela da suite (pytest -n auto): gate_fase do orquestrador 4F
pytest-xdist==3.8.0

# Linters e Qualidade
flake8==7.3.0
black==26.5.1
mypy==2.3.1

# Runner dos Quality Gates (NIH #4 — 'audit' delega para pre-commit)
pre-commit==4.6.2

# Ferramental de Segurança e Auditoria IaC
bandit==1.9.4
pip-audit==2.10.1
detect-secrets==1.5.0
# checkov NAO entra aqui: ele exige importlib-metadata<8 e o litellm sem
# vulnerabilidades conhecidas exige >=8 — os dois nao coexistem na mesma
# instalacao. Desde 2026-09-19 o checkov vive no venv isolado do hook
# 'g-infra-compose' (.pre-commit-config.yaml, additional_dependencies).
# Para rodar o scanner a mao, sem sujar este ambiente:
#   pipx run checkov==3.3.19 -f <arquivo>
