# Dependências do Ecossistema AIDD Unificado
#
# PLAN-0018 fase 04 (pin-exato-e-hashes): pins exatos (==), sem faixas soltas
# (>=, ~=, <). A instalação real e verificada em CI/pre-commit usa o
# lockfile com hashes criptográficos gerado a partir deste arquivo:
#   uv pip compile requirements.txt --generate-hashes -o requirements.lock
# Para atualizar uma versão: edite o pin abaixo e regenere requirements.lock.
pytest==9.1.1
pytest-cov==7.1.0
hypothesis==6.168.0
jsonschema==4.26.0
# litellm: destravado em 2026-09-19. A versao anterior (1.83.0) acumulava 7
# vulnerabilidades catalogadas (PYSEC-2026-388/391/2598..2602), entre elas uma
# falsificacao de requisicao no lado do servidor. A trava existia porque o checkov
# exige importlib-metadata<8 e o litellm corrigido exige >=8; a solucao foi mover o
# checkov para o venv isolado do hook 'g-infra-compose' em vez de manter um pacote
# vulneravel. Superficie de uso no codigo: apenas litellm.completion()
# (tools/aidd-generator/scripts/phases/utils_delegacao.py), contrato estavel.
litellm==1.101.0
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
python-dotenv==1.2.3
requests==2.34.2
tiktoken==0.14.0
click==8.5.0
colorama==0.4.6
secure==2.0.1
sqlglot==30.18.0
sqlalchemy==2.0.52
alembic==1.20.0
returns==0.29.0
cookiecutter==2.7.1
pyyaml==6.0.3

# aidd-bridge: deploy e teardown em VPS via SSH/SFTP
paramiko==5.0.0

# aidd-bridge: geração de JWT assinado para GoTrue/PostgREST (anon + service_role)
PyJWT==2.14.0
cryptography==50.0.1

# aidd-bridge: migração de contas reais (auth.users/auth.identities) de um
# Supabase Cloud de origem para o Postgres self-hosted de destino
psycopg2-binary==2.9.13

# observabilidade: OpenTelemetry + structlog
opentelemetry-api==1.33.0
opentelemetry-sdk==1.33.0
opentelemetry-instrumentation-fastapi==0.54b0
opentelemetry-exporter-otlp==1.33.0
structlog==25.4.0

# asyncpg: driver assíncrono de alto desempenho para PostgreSQL
# usado por AsyncPostgresAdapter para I/O não-bloqueante
# 0.30.0 -> 0.31.0 em 2026-09-19: a 0.30.0 nao publica pacote pronto para Python
# 3.14, entao a instalacao tentava compilar do fonte e exigia Visual Studio — o
# ambiente declarado simplesmente nao subia em maquina Windows com 3.14.
asyncpg==0.31.0

# skill aidd-livro-texto: composicao de livro-texto em PDF (pandoc + typst).
# Sao os dois programas empacotados como modulo Python, para que a geracao de
# documento funcione em maquina onde nao se pode instalar software do sistema
# (CI, container enxuto, estacao corporativa travada). Quando pandoc/typst ja
# existem no PATH, o motor da skill usa os do sistema e ignora estes pacotes.
# Detalhe operacional: o pacote `typst` expoe API Python e NAO instala executavel,
# por isso o motor compoe em dois passos nesse caso (ver scripts/livro.py).
pypandoc-binary==1.17
typst==0.15.0
