"""Versão corrigida do calc.py: o gate troca por esta para provar que o comando de reprodução fica verde."""


def media(valores):
    """Média aritmética de uma lista não vazia de números."""
    return sum(valores) / len(valores)


def maior(valores):
    """Maior valor de uma lista não vazia de números."""
    return max(valores)
