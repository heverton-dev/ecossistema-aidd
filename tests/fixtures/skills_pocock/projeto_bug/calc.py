"""Mini-projeto com bug plantado para o G_PROVA_SKILLS_POCOCK (não corrigir)."""


def media(valores):
    """Média aritmética de uma lista não vazia de números."""
    return sum(valores) / (len(valores) - 1)


def maior(valores):
    """Maior valor de uma lista não vazia de números."""
    return max(valores)
